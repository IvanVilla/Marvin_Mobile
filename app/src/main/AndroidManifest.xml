<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.klaussius.marvin_mobile">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginForm"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenu" />
        <activity android:name=".SignInForm" />
        <activity android:name=".ListTournaments" />
        <activity android:name=".Working" />
        <activity android:name=".Profile" />
        <activity android:name=".ProfileDeleteConfirmation" />
        <activity android:name=".ProfileEdit" />
        <activity android:name=".MenuTournament" />
        <activity android:name=".EulaForm" />
        <activity android:name=".TournamentProgress" />
        <activity android:name=".PrizesList" />
        <activity android:name=".ListTournamentsInProgress"></activity>
    </application>

</manifest>